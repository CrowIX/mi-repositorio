'use strict';

/**
 * *Google Chrome usar el motor V8 para interpretar JavaScript
 * *Lee linea linea y detecta errores.
 * !El CONTEXTO DE EJECUCION es diferente a un AMBITO DE EJECUCION.
 * *Una funcion al ser llamada crea un contexto de ejecucion que esta por encima de el contexto global
 * *Y estos contextos se van apilando en la pila de ejecucion y se  ejecutara primero de arriba a abajo.
 * *
 */
